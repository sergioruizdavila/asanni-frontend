/**
* config()
* @description - rating config file
*/

(function() {
    'use strict';

    angular
        .module('mainApp.components.rating', [])
        .config(config);

    function config() {}
})();
