/**
* config()
* @description - header config file
*/

(function() {
    'use strict';

    angular
        .module('mainApp.components.header', [])
        .config(config);

    function config() {}
})();
