/**
* config()
* @description - float Message Bar config file
*/

(function() {
    'use strict';

    angular
        .module('mainApp.components.floatMessageBar', [])
        .config(config);

    function config() {}
})();
