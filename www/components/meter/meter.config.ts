/**
* config()
* @description - meter config file
*/

(function() {
    'use strict';

    angular
        .module('mainApp.components.meter', [])
        .config(config);

    function config() {}
})();
