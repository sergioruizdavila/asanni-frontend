/**
* config()
* @description - survey config file
*/

(function() {
    'use strict';

    angular
        .module('mainApp.components.survey', [])
        .config(config);

    function config() {}
})();
