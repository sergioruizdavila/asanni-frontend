/**
 * config()
 * @description - modal config file - Here init modal called dependencies
 */

(function () {
    'use strict';

    angular
        .module('mainApp.components.modal', [])
        .config(config);

    function config() { }
})();
