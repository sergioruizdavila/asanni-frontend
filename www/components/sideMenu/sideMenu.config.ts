/**
* config()
* @description - sideMenu config file
*/

(function() {
    'use strict';

    angular
        .module('mainApp.components.sideMenu', [])
        .config(config);

    function config() {}
})();
